# 제품 요구사항 정의서 (PRD) v2.0
## LibAR — 도서관 빅데이터 × 책등 인식 기반 실시간 AR 서가 관리·탐색 시스템

> **공모전:** 2026 도서관 데이터 활용 공모전 (국립중앙도서관 주최)
> **응모 부문:** ✅ **서비스 아이디어 제안** (확정, 데이터 활용 및 분석 기반)
> **팀 구성:** 2인 — 개발/기획 1인 + **현직 사서 1인(솔로몬 접근 가능)**
> **1차 원고 마감:** 2026. 8. 7.(금) 24:00 | **2차 발표·시연:** 2026. 8. 28.(금)
> **작성일:** 2026. 7. 2. (마감까지 D-36)

---

## 0. 공모전 요건 준수 체크 (심사 제외 방지 — 최우선)

요강(붙임3)에 따라 **도서관데이터 1개 이상 필수 활용, 미활용 시 심사 제외**.
v1.0 PRD는 자체 생성 데이터만 사용하여 결격 사유였음 → v2.0에서 아래와 같이 해소.

| 요건 | 대응 | 상태 |
| :--- | :--- | :--- |
| 도서관데이터 필수 활용 | **솔로몬(Solomon) 실제 장서·대출 데이터**(사서 팀원 접근) + **정보나루 Open API** + **국립중앙도서관 Open API**(보강) | 본 문서 §4 |
| AI 기술 사용 시 종류·산출물 명시 | 원고에 "AI 활용 명세" 절 포함 (§12) | 반영 |
| 원고 A4 15매 이내(hwp), 요약문 1매 | §11 원고 목차 설계에 반영 | 반영 |
| 원고에 개인정보 기재 금지 (실격) | 원고 작성 시 이름·연락처 제거 체크리스트 | §11 |
| 2차: 분석에 활용한 데이터 및 모델 1식 제출 | 학습 데이터셋 + 모델 가중치 + 분석 노트북을 저장소로 패키징 | §10 |
| 타 공모전 중복 출품 금지 | 순수 신규 개발 | 확인 |

**부문 선택 근거:** 본 프로젝트는 "도서관 데이터 기반의 사회적 현안(장서 점검 인력난, 이용자 탐색 실패) 해결 모델"이므로 **① 서비스 아이디어 제안** 부문에 부합. (② 융합콘텐츠 부문도 AR이 명시돼 있으나, 결과물이 '콘텐츠 파일'이 아닌 '서비스+분석 모델'이므로 ①이 적합. 부문별 중복지원 금지이므로 ①로 확정.)

---

## 1. 프로젝트 개요

- **제품명:** LibAR (립아르) — 책등 인식 기반 실시간 AR 도서 관리·가이드 시스템
- **한 줄 정의:** 스마트폰 카메라로 서가를 비추면, 온디바이스 AI가 책등을 탐지·청구기호를 판독하여 **사서에게는 오배열 도서(빨강)를, 이용자에게는 찾는 책(초록)을** 화면 위에 실시간 표시하고, 축적된 오배열 로그를 **전국 공공도서관 대출 빅데이터(정보나루)와 결합 분석**하여 서가 운영을 최적화하는 PWA 서비스.
- **실증(파일럿) 대상:** **영등포구립 대림도서관**. 솔로몬 실제 장서·대출 데이터로 서비스를 구동·검증하되, 이 파이프라인은 특정 관에 종속되지 않고 **솔로몬 참여 전국 1,576개 공공도서관에 동일 적용 가능한 표준 모델**. → 대림도서관은 전국 확산을 위한 최초 실증 사례(발표심사 '활용성·확산 용이성' 정면 공략).

### 1.1 문제 정의 (데이터로 뒷받침)
1. **장서 점검의 고비용:** 바코드/RFID 점검은 개별 도서를 1권씩 스캔해야 함. RFID는 태그·리더·게이트 등 관당 수천만 원의 도입 비용이 들어 중소 도서관·학교도서관은 도입률이 낮음. → 원고에서 국가도서관통계시스템(libsta.go.kr)의 관당 장서 수·직원 수 통계로 "직원 1인당 점검 부담"을 정량화.
2. **'Last 1-meter' 문제:** 이용자는 OPAC 검색으로 청구기호까지는 얻지만, 실제 서가 앞에서 책을 못 찾음. 오배열된 책은 사실상 '유령 장서'가 됨.
3. **오배열은 측정된 적이 없는 데이터:** 어느 서가에서, 어떤 주제의 책이, 얼마나 자주 오배열되는지에 대한 데이터 자체가 부재 → LibAR가 세계 최초 수준의 '오배열 로그 데이터셋'을 생성하고, 이를 대출 빅데이터와 교차 분석.

### 1.2 차별성 (심사항목 '창의성 30점' 대응 — 선행사례 조사 완료 2026.7.2.)

**포지셔닝 원칙:** "세계 최초" 주장 금지. 아래 선행사례를 원고에 먼저 인용한 뒤 차별점을 제시하는 것이 '기존 사례와의 차별성' 항목에서 오히려 유리. 특히 교세라의 상용화 성공은 **시장 수요가 실재한다는 증거**로 기대효과 절에 활용.

| 선행 사례 | 방식 | 한계 | LibAR 차별점 |
| :--- | :--- | :--- | :--- |
| **교세라 SHELF EYE** (일본, 상용, 최유사 사례) | 태블릿 촬영 → AI 책등 인식 → ISBN 특정·장서 대조, 오배열 확인 기능 보유. "바코드 대비 작업시간 절반 이하" 공표 | 촬영 후 일괄 분석(실시간 AR 미지원, 스마트글래스 '검토 중'), 사서 전용(이용자 기능 없음), B2B 유료 | ① 실시간 AR 오버레이 ② 이용자 모드(Last 1-meter) ③ 무료 PWA ④ **대출 빅데이터 매쉬업 분석** |
| **ShelvAR** (미국, 2011, 중단) | 사서용 AR 오배열 표시 — 개념 동일 | 책등에 **전용 코드 태그를 전량 부착**해야 함(RFID와 같은 노동), 미국 특허 문제로 상용화 실패·중단 | 태그 불필요 — 기존 청구기호 라벨을 OCR로 직접 판독 |
| RFID 장서점검 로봇 (국내 주류: 이순신도서관 등, 인식률 ~93%) | 로봇 + RFID | 관당 수천만 원, 태그 부착, 서가 통로 폭 제약 | 추가 하드웨어 0원, 스마트폰만 |
| 일본 카메라 기반 점검 (IBIS 드론, KoKoBo 로봇, 나카바야시 카메레온코드) | 드론/로봇/컬러코드 라벨 | 전용 장비 또는 전용 라벨 필요 | 범용 스마트폰 + 기존 라벨 |
| 소비자 책등 스캔 앱 (Shelf Scan, ShelfWise, BiblioScan 등 다수) | 사진 1장 → AI 책등 인식 | 개인 장서 목록화·중고판매용. 청구기호/KDC 검증·오배열·도서관 운영 기능 없음 | 도서관 도메인 로직(KDC 정렬 검증·오배열 판정·점검 워크플로) |
| 책등 인식 학술연구 (Smart Library JCDL'17 등) | 논문 단계 | 인식에서 종료, 서비스·데이터 활용 부재 | 인식 → 로그 축적 → **정보나루 대출 데이터 상관분석 → 점검 우선순위 예측**까지 완결 |

**국내 공백 확인:** 국내 상용 카메라·OCR 기반 장서점검 서비스 및 서가 앞 AR 책찾기 서비스는 미확인(2026.7. 조사 기준). 본 공모전 과거 수상작에도 동일 아이디어 미확인. → **"오배열 로그 × 전국 대출 빅데이터 결합 분석"은 세계 어느 사례에도 없는 본 제안 고유의 기여**이며, 이것이 '도서관 데이터 활용' 공모전 취지와의 접점.

**선행사례 출처 (원고 참고문헌용, APA 변환 필요):**
- SHELF EYE: https://www.kyocera.co.jp/intellectual-property/news/2024/07-01-176.html / https://www.itmedia.co.jp/news/articles/2102/25/news084.html / http://smartiot-forum.nict.go.jp/iot-val-team/iot-case/kccs/
- ShelvAR: http://www.shelvar.com/ / https://www.popsci.com/technology/article/2011-04/augmented-reality-app-librarians-instantly-shows-which-books-are-misfiled/
- 국내 RFID 장서점검 로봇(이순신도서관): https://cnclib.com/article/library-news/2/2382/ / K-ROBOT: https://www.cucon.co.kr/k-solution/k-robot
- 일본 기타: KoKoBo https://www.shidax.co.jp/corporate/press-release/2024/090501/ / IBIS(닛케이) https://www.nikkei.com/article/DGXMZO56710820S0A310C2L83000/ / 카메레온코드 https://nakabayashi-library.site/n-class/smart-zouten.html
- 소비자 앱: Shelf Scan https://apps.apple.com/us/app/shelf-scan-book-spine-scanner/id6754824642 / ShelfWise https://www.shelfwise.io/ / BiblioScan https://biblioscan.ai/en
- 연구: Smart Library (JCDL 2017) https://zihan-z.github.io/paper/JCDL17-SmartLibrary.pdf

---

## 2. 타겟 유저 및 핵심 가치

### 2.1 사서 (Admin Mode)
- **페인 포인트:** 정기 장서 점검 시 수천 권의 청구기호를 육안 대조. 인력 부족으로 점검 주기가 늘어지고, 그 사이 오배열 도서는 '소장하지만 찾을 수 없는 책'이 됨.
- **핵심 가치:**
  1. 서가를 스캔하며 지나가면 오배열이 즉시 빨간 박스로 표시 (탐지 자동화)
  2. **점검 우선순위 추천:** 정보나루 대출 데이터 기반 "대출 회전율이 높아 오배열 위험이 큰 서가"부터 점검하도록 히트맵 제공 (데이터 기반 의사결정)

### 2.2 이용자 (User Mode)
- **페인 포인트:** 청구기호를 알아도 서가 앞에서 헤맴. KDC 배열 규칙을 모르는 어린이·고령자·외국인은 더 취약. → **정보취약계층 정보격차 해소**(요강의 주제 예시와 직결)라는 프레임으로 원고 서술.
- **핵심 가치:**
  1. 검색한 책이 카메라 화면에서 초록색 펄스로 하이라이트
  2. 검색 시 정보나루·국중 API로 **서지정보 + 전국 인기대출 순위 + 사서추천 여부**를 AR 카드로 표시 (도서관 데이터의 이용자 접점 확대)

---

## 3. 핵심 기능 요구사항

각 기능에 우선순위 태그 부여: **[MVP]** = 8/7 원고 시점까지 동작 증빙 필요, **[DEMO]** = 8/28 시연 시점까지, **[VISION]** = 원고에는 설계만 제시.

### 3.1 [Epic 0] 도서관 데이터 연동 파이프라인 (신규 — 필수요건이자 적합성 40점의 핵심)

- **FR-0.1 [MVP]** 도서관 정보나루 Open API 인증키를 발급받고, 다음 API를 연동한다:
  - `srchDtlList` (도서 상세 조회): ISBN → 서지·주제분류(KDC) 획득
  - `loanItemSrch` (인기대출도서 조회): 지역·연령별 인기대출 순위
  - `usageAnalysisList` (도서별 이용 분석): 도서별 대출 추이·다대출 이용자군
  - `itemSrch` (도서관별 장서/대출 조회): 특정 도서관의 소장·대출 데이터
  - ※ API 명세는 정보나루(data4library.kr) 로그인 → 인증키 발급 페이지에서 최종 확인 후 확정.
- **FR-0.2 [MVP]** 시연 대상 도서관의 장서 목록을 **솔로몬 장서데이터(사서 팀원, 실제 청구기호·배가 포함)로 우선 확보**하고, 미접근 시 정보나루 도서관별 장서 개방 데이터(CSV)로 대체하여 Supabase `books` 테이블에 적재한다. → OCR로 읽은 청구기호를 이 테이블과 대조하여 "실제 소장 도서" 검증 및 서지 매칭. (익명·집계 형태 준수)
- **FR-0.3 [DEMO]** 국립중앙도서관 Open API(ISBN 서지정보, 사서추천도서)를 이용자 모드 도서 상세 카드에 연동한다.
- **FR-0.4 [DEMO]** **매쉬업 분석 모델:** 자체 축적한 오배열 로그 × 정보나루 대출 빅데이터를 결합하여 다음 가설을 검증하고 원고·발표에 수록한다:
  - H1: 대출 빈도가 높은 주제(KDC 대분류)일수록 오배열률이 높다 (상관분석)
  - H2: 어린이 자료(아동 KDC)의 오배열률이 일반 자료보다 높다
  - 산출물: "서가별 오배열 위험도 스코어 = f(대출 회전율, 과거 오배열 밀도)" → 점검 우선순위 히트맵

### 3.2 [Epic 1] 실시간 비전 인식 — 책등 탐지 (Vision)

- **FR-1.1 [MVP]** 스마트폰 후면 카메라 스트림을 브라우저에서 캡처한다 (`getUserMedia`, 해상도 1280×720 기준).
- **FR-1.2 [MVP]** Ultralytics YOLO 경량 모델로 프레임 내 '책등(spine)' 객체를 탐지하고 좌표와 신뢰도를 반환한다.
  - **모델 선택 — YOLO26n 우선, YOLO11n 폴백:**
    - **1순위 YOLO26n** (2026.1 출시 최신): ① **NMS-free(end-to-end)** → 밀집·경사 책등에서 인접 박스 겹침을 NMS가 지워버리는 문제 자체가 없음(본 데이터에 결정적) ② CPU ONNX 추론 YOLO11n 대비 최대 43% 빠름 → 브라우저 온디바이스 실시간에 유리 ③ STAL로 작은 표적(청구기호 라벨 스티커) 인식↑ ④ OBB·ONNX 내보내기 지원
    - **폴백 YOLO11n**: 생태계 성숙(튜토리얼·버그픽스 다수). YOLO26n의 브라우저(`onnxruntime-web`) 배포에 이슈가 있으면 즉시 전환(데이터·코드 Ultralytics 공유라 교체 무비용)
    - **검출 방식:** 기본 검출(수평 박스)로 PoC → 기울어진 책등의 박스 겹침·순서 오류가 실측되면 **OBB(회전 박스)** 로 전환. OBB는 각도(θ)도 출력해 흔들림 보정에 유리
    - **라벨 클래스:** ① 책등(spine, 순서 계산용) ② 청구기호 라벨 스티커(OCR 대상 영역) 2클래스 권장
    - ⚠ **W2 첫날 스모크 테스트:** YOLO26n → ONNX → `onnxruntime-web`에서 프레임 추론이 뜨는지부터 확인 후 확정
  - **학습 데이터 계획 (구체):**
    1. 공개 데이터셋(출발점): Roboflow Universe book-spine 계열 — [Book spline detection 1.5k장](https://universe.roboflow.com/books-26cz6/book-spline-detection), [Book Spine Detector](https://universe.roboflow.com/bookdetection-lgtpa/book-spine-detector) 등. 데이터셋은 YOLO 버전 무관하게 재사용 가능
    2. **실제 데이터(핵심) — 영상 촬영 방식:** 대림도서관 서가를 팀원이 **5~10분 워크스루 영상** 촬영 → Roboflow에서 **프레임 추출(초당 1~2장)** 로 수백 장 확보 (개별 사진 촬영 불필요). 여러 서가·층·조도·각도로 다양성 확보
    3. **라벨링 — 반자동:** Roboflow Auto Label(Grounding DINO) 또는 공개 모델로 자동 프리라벨 → 사람이 수정, Repeat Previous로 연속 프레임 가속 (수백 장이 몇 시간)
    4. 증강: 회전(±10°)·밝기·블러·노이즈로 확장(이미지당 최대 수십 배 자동 생성)
  - **학습 환경:** Google Colab (무료 T4), Ultralytics 프레임워크, 640px 입력, 100 epoch 기준 수 시간 내 학습 가능
  - **검증 기준:** 자체 테스트셋에서 mAP@0.5 ≥ 0.85 (미달 시 데이터 추가 촬영 1회 반복)
- **FR-1.3 [MVP]** 모델을 ONNX로 내보내고 브라우저에서 `onnxruntime-web`(WASM/WebGPU)으로 온디바이스 추론한다. INT8 양자화 버전을 함께 빌드하여 속도/정확도 비교 후 채택.
- **FR-1.4 [DEMO]** 추론은 초당 2~5회(프레임 스킵)로 수행하고, 그 사이 프레임은 §3.4의 트래킹으로 보간하여 화면 렌더링은 30fps를 유지한다.

### 3.3 [Epic 2] 청구기호 판독 및 KDC 정렬 검증 (OCR & Logic)

- **FR-2.1 [MVP]** 탐지된 각 책등 크롭 이미지에서 청구기호 라벨 텍스트를 OCR로 추출한다.
  - **1차 채택:** **한국어 PP-OCRv5** (`korean_PP-OCRv5_mobile_rec`, HF) — 한국어 전용·v3 대비 +30% 정확도·경량. **샘플 실측(라벨 10개): 평균 신뢰도 EasyOCR 0.71 → PP-OCRv5 0.97, 최저 0.51→0.93.** Windows/Python 3.13 설치 확인(paddlepaddle 3.3.1). 서버 배포 시 FastAPI(HF Spaces/Cloud Run 무료 티어). ※ paddle 3.3 CPU는 oneDNN(PIR) 버그로 `FLAGS_use_mkldnn=0` 필요
  - **개발용 경량 기본:** EasyOCR (설치 가벼움) — 샘플은 `--ocr` 플래그로 두 엔진 전환·비교 지원
  - **폴백:** 정확도 미달 시 네이버 CLOVA OCR 무료 크레딧으로 교체 (API 호환 어댑터 계층을 둬서 교체 비용 최소화)
  - **후처리:** 청구기호 정규식(`^\d{1,3}(\.\d+)?\s*[가-힣A-Za-z]\d*[가-힣A-Za-z]?(\s*[vV]?\.?\d+)?`) 기반 노이즈 제거 + 유사문자 교정표(O↔0, l↔1 등) 적용
- **FR-2.2 [MVP]** 같은 선반(row) 내 도서를 y좌표로 클러스터링 후 x좌표 오름차순으로 물리적 배열 순서를 확정한다.
- **FR-2.3 [MVP]** 청구기호를 구조체로 파싱하고 KDC 배열 규칙으로 비교한다:
  ```
  파싱: "813.7 김94ㅎ v.2" → {분류번호: 813.7, 저자기호: "김94ㅎ", 권차: 2}
  비교: 분류번호(수치) → 저자기호(가나다+숫자 혼합 정렬) → 권차(수치) 순 사전식 비교
  ```
- **FR-2.4 [MVP]** **오배열 판정 알고리즘 — 최장 증가 부분 수열(LIS):** 단순 인접 비교는 1권의 오배열이 이웃 정상 도서까지 오탐시키므로, 배열 전체에서 LIS를 구해 **LIS에 속하지 않는 최소 집합만** 오배열로 판정한다. (n≤50/선반이므로 O(n²)로 충분)
  - 검증: 유닛 테스트 — 정상 배열, 1권 오배열, 2권 맞교환, 권차 역순 등 10개 케이스 통과
- **FR-2.5 [DEMO]** OCR 신뢰도가 임계값 미만인 책등은 '판독 불가(회색)'로 구분 표시하여 오탐을 사용자에게 전가하지 않는다.
- **FR-2.6 [MVP]** **기준표 대조 + 이중 증거 매칭 (샘플에 구현·검증 완료)** — OCR 원본을 직접 신뢰하지 않고, 시연 서가 기준표(장서데이터)가 후보를 제약하는 구조:
  - **증거 1(청구기호):** ① 조각이 기준표에서 유일하면 확정 ② 분류번호가 읽히면 동일 분류 후보로 좁혀 저자기호 퍼지 매칭 ③ 여러 책과 모호하면 매칭 포기
  - **증거 2(제목):** 책등 제목 영역 OCR(세로쓰기 대응 3방향 시도) → 기준표 서명과 부분문자열 매칭
  - **결합:** 두 증거 일치 시 확신도 최상. 라벨 잘림·훼손으로 증거 1이 실패/모호하면 **증거 2로 복구**. 둘 다 실패 시에만 '판독 불가'. → 얇은 책 라벨 감김·훼손·동일 분류 중복에 강건 (현직 사서 현장 관찰 반영)

### 3.4 [Epic 3] AR 시각 오버레이 (UI)

- **FR-3.1 [MVP]** 오배열 판정 도서 위에 **붉은색 반투명 박스 + ⚠ 아이콘**, 판정 근거(예: "813.7 ← 여기 있어야 함: 815.4 뒤")를 탭 시 표시.
- **FR-3.2 [MVP]** 이용자 검색 도서와 청구기호 매칭 시 **초록색 펄스 박스** + 상단에 서지 카드(표지·제목·저자, 국중/정보나루 API 데이터).
- **FR-3.3 [DEMO]** 흔들림 보정: 프레임 간 IoU 기반 박스 매칭(간이 트래커) + 좌표 지수이동평균(EMA, α≈0.4) 스무딩. 렌더링은 HTML5 Canvas 단일 레이어(Three.js는 불필요 — 과설계 방지).
- **FR-3.4 [VISION]** WebXR 기반 서가 간 길안내(층·구역 내비게이션) — 원고에 발전방향으로만 제시.

### 3.5 [Epic 4] 오배열 데이터 축적 및 분석 대시보드 (Data & Analytics)

- **FR-4.1 [MVP]** 오배열 탐지 시 `{청구기호, ISBN(매칭 시), 탐지시각, 서가 Zone ID, 스캔 세션 ID}`를 Supabase에 로깅.
- **FR-4.2 [DEMO]** 관리자 대시보드 (Next.js + Recharts):
  - 일별/주별 오배열 추이, KDC 대분류별 오배열 분포
  - **핫스팟 히트맵:** 서가 Zone × 오배열 밀도
  - **점검 우선순위 위젯:** FR-0.4의 위험도 스코어 상위 서가 Top 5
- **FR-4.3 [DEMO]** 사서 조치 워크플로: 오배열 항목에 '재배열 완료' 체크 → `is_resolved` 갱신, 미조치 건 리마인드 목록.

---

## 4. 활용 데이터 명세 (원고 요약문 '활용 도서관데이터' 절에 그대로 사용)

| 구분 | 데이터/API | 용도 | 요건 분류 |
| :--- | :--- | :--- | :--- |
| **도서관 빅데이터(솔로몬)** | **솔로몬 장서데이터 18종·연계데이터** — **영등포구립 대림도서관(실증 대상, 사서 팀원 접근)** 실제 청구기호·배가·대출 | **정답 배열 기준표(`books`) 구축, 오배열 판정, 오배열×대출 상관분석(핵심)** | **필수요건 충족·최우선** |
| 도서관 빅데이터 | 정보나루 Open API 4종 (도서 상세, 인기대출, 이용분석, 도서관별 장서) | 서지 매칭, AR 카드, 전국 단위 매쉬업 분석 | **필수요건 충족** |
| 도서관 빅데이터 | 정보나루 도서관별 장서/대출 개방 데이터(CSV) | 솔로몬 접근 불가 시 대체 장서 DB | 백업 |
| 국립중앙도서관 | Open API (ISBN 서지정보, 사서추천도서) | 도서 상세 카드 보강 | 보강 |
| 개방 데이터 | 국가도서관통계시스템(libsta.go.kr) 통계 | 문제 정의 정량화 (관당 장서/인력) | 매쉬업 |
| 자체 생성 | LibAR 오배열 로그 | 위험도 스코어 모델 학습·검증 | 신규 가치 |

### 4.1 인증키·데이터 접근 확보 절차 (W1 최우선 — 오늘 착수)

| # | 데이터원 | 담당 | 절차 | 승인/제한 | 상태 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | **정보나루 Open API** (data4library.kr) | 개발자 | 회원가입 → `데이터 활용` 메뉴 → 인증키 이용신청(사용목적: 본 공모전 출품작 개발) | **익일 오전 승인**. 기본 500회/일 → 마이페이지에서 **서버IP 등록 시 30,000회/일** | ☐ 신청 |
| 2 | **국립중앙도서관 Open API** (nl.go.kr) | 개발자 | 로그인 → `신청·참여` → `연계 API` → `Open API 인증키 신청하기` → 발급(담당자 승인) | 승인 후 사용 | ☐ 신청 |
| 3 | **솔로몬(Solomon)** (solomon.data4library.kr) | **사서 팀원** | **대림도서관** 직원 계정 로그인 → 장서데이터·대출데이터 export → [활용 매뉴얼](https://data4library.kr/noticeV?cn=127) 참고 → 소속관에 **데이터 반출 범위·규정 사전 확인** | 참여도서관 직원만 접속. 대림 데이터가 보이면 참여관 확정(안 보이면 정보나루 개방 CSV 대체) | ☐ 확인 |

> ⚠ **개인정보/보안 원칙:** 솔로몬 회원·대출 데이터는 민감정보 포함. 원고·발표·제출물에는 **집계·익명화 형태만** 사용. 소속관 반출 규정 위반 금지. 요강상 원고 내 개인정보 기재는 실격.
> ⚠ 인증키 승인 대기(익일)를 고려해 **W1 첫날 3건 모두 신청**. 승인 지연 시 정보나루 개방 CSV(키 불필요)로 분석 선진행.

---

## 5. 기술 아키텍처

### 5.0 처리 파이프라인 v2.1 — 현장 우려(판본·라벨·그림책) 반영 재설계

```
[한 컷 스캔 모드 = 기본]  (실시간 모드는 이 위에 프레임 반복 + 트래킹만 추가)

사진 1장
 → ① 탐지: 책등 + 청구기호 라벨 스티커 (YOLO26n, 폴백 YOLO11n / 라벨은 CV 보조 탐지)
 → ② 라벨 크롭 → OCR (어댑터 계층: EasyOCR/PaddleOCR ↔ CLOVA 교체 가능)
 → ③ 신뢰도 게이트: 판독 신뢰도 미달 → '판독 불가(회색)' 분리, 판정에서 제외
 → ④ 정답 기준표(books.csv ← 솔로몬/OPAC 장서데이터) 대조 → 소장 도서 매칭
 → ⑤ LIS 오배열 판정 (인접 비교 아님 — 최소 오배열 집합만 지목)
 → ⑥ 결과 오버레이: 🔴 오배열 / 🟢 검색 도서 / ⚪ 판독 불가 / 🔵 기준표 미등록
```

**v2.1 설계 결정 (우려 → 반영):**
| 현장 우려 | 설계 반영 |
| :--- | :--- |
| 판본·개정판·커버 탈착으로 동일 여부 판단 곤란 | **라벨 우선(Label-first)**: 책 생김새 식별을 아예 하지 않음. 청구기호 라벨 텍스트만 읽음 → 판본 문제 원천 회피 |
| 라벨 훼손·반사로 오독 위험 | **③ 신뢰도 게이트 정식 단계화**: 미달 시 회색 처리, 오배열 판정에 불참(오탐보다 미탐이 안전) |
| 어린이 그림책 등 비정형 배가 | **Scope 가드(§9.1)**: spine-out 단행본 서가만 대상. 면출·바구니 배가는 파이프라인 진입 전 제외 |
| 한 권 오배열이 이웃까지 오탐 | ⑤ LIS로 최소 집합만 지목 |
| 실시간 성능 불확실 | **한 컷 모드를 기본**으로 두고 실시간은 그 위에 증축 → 데모 안정성 확보 |

> **샘플 구현체:** `libar-sample/` (가정용 책 10권 시연 파이프라인, 데이터만 교체하면 대림 서가에 그대로 적용)

```
[클라이언트: Next.js 14 PWA (Vercel 무료 배포)]
 ├─ 카메라 캡처: getUserMedia → OffscreenCanvas
 ├─ 책등 탐지: onnxruntime-web (YOLO26n 우선/11n 폴백, NMS-free, INT8, WASM/WebGPU) ← 온디바이스
 ├─ 트래킹·스무딩: IoU 매처 + EMA (자체 구현, ~100줄)
 └─ AR 렌더: HTML5 Canvas 오버레이
        │ (탐지 크롭 이미지, 초당 2~5회)
        ▼
[OCR 서버: FastAPI + PaddleOCR-korean (HF Spaces/Cloud Run 무료 티어)]
 └─ 청구기호 추출 → 정규식 후처리 → KDC 파싱
        │
        ▼
[백엔드: Supabase (무료 티어)]
 ├─ PostgreSQL: books(정보나루 장서), misplacements, scan_sessions
 ├─ Edge Functions: 정보나루/국중 API 프록시 (인증키 은닉)
 └─ Realtime: 대시보드 실시간 갱신
        │
        ▼
[대시보드: 같은 Next.js 앱 /admin 라우트 + Recharts]
```

**설계 원칙:** ① 전 구간 무료 티어로 운영 가능(지속가능성 → 활용성 40점) ② 탐지는 온디바이스로 서버 비용·지연 최소화 ③ OCR만 서버 위임(모바일 브라우저에서 한국어 OCR은 비현실적).

### 5.1 데이터베이스 스키마

```sql
-- 시연 도서관 장서 (솔로몬 실제 장서데이터 우선, 미접근 시 정보나루 개방 데이터)
CREATE TABLE books (
  isbn13        VARCHAR(13) PRIMARY KEY,
  title         TEXT NOT NULL,
  author        TEXT,
  call_number   VARCHAR(50) NOT NULL,   -- 청구기호 (매칭 키)
  kdc_class     VARCHAR(10),            -- KDC 대분류 (분석용)
  loan_count    INT DEFAULT 0,          -- 정보나루 대출 횟수 (분석용)
  zone_id       VARCHAR(20)             -- 배가 구역
);
CREATE INDEX idx_books_call_number ON books (call_number);

-- 스캔 세션 (점검 시간 측정 → 성과 입증용)
CREATE TABLE scan_sessions (
  id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  started_at    TIMESTAMPTZ DEFAULT NOW(),
  ended_at      TIMESTAMPTZ,
  zone_id       VARCHAR(20),
  books_scanned INT DEFAULT 0
);

-- 오배열 탐지 로그
CREATE TABLE misplacements (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id      UUID REFERENCES scan_sessions(id),
  created_at      TIMESTAMPTZ DEFAULT NOW(),
  call_number     VARCHAR(50) NOT NULL,
  isbn13          VARCHAR(13) REFERENCES books(isbn13),
  expected_between TEXT,                -- "815.4 뒤 ~ 816.1 앞"
  zone_id         VARCHAR(20) NOT NULL,
  ocr_confidence  REAL,
  is_resolved     BOOLEAN DEFAULT FALSE,
  resolved_at     TIMESTAMPTZ
);
```

---

## 6. 비기능 요구사항 (현실화된 수치)

| 항목 | 목표 | 측정 방법 |
| :--- | :--- | :--- |
| 탐지 추론 속도 | 온디바이스 2~5 inference/s (중급 안드로이드 기준) | Galaxy A 시리즈 실기기 측정 |
| 화면 렌더링 | 트래킹 보간으로 체감 30fps | DevTools 프레임 프로파일 |
| OCR 왕복 지연 | 크롭 배치 1회당 < 2s | 서버 로그 |
| 책등 탐지 정확도 | mAP@0.5 ≥ 0.85 (자체 테스트셋) | Ultralytics val |
| 청구기호 판독 정확도 | 라벨 단위 정확도 ≥ 90% (통제 조명 기준) | 모의 서가 100권 실측 |
| 접근성 | QR 접속 → 3탭 이내 스캔 시작, 앱 설치 불필요(PWA) | 시나리오 테스트 |

> v1.0의 "15fps 실시간 비전 처리"는 모바일 브라우저에서 비현실적 → "저빈도 추론 + 트래킹 보간" 구조로 목표 재정의. 발표 Q&A에서 기술적 정직성으로 방어 가능.

---

## 7. 성과 검증 실험 설계 (기대효과 30점 — 주장이 아닌 실측으로)

**실험: 모의 서가 점검 시간 비교 (Within-subject)**
- 셋업: 책 100권 서가(3단), 무작위 10권 오배열 배치 (배치표는 실험자와 분리 작성)
- 조건 A: 육안 점검 (청구기호 목록 대조) / 조건 B: LibAR 스캔
- 측정: ① 점검 소요 시간 ② 오배열 검출 재현율(놓친 것) ③ 오탐률
- 참가자: 3명 이상 × 2조건 (순서 카운터밸런싱)
- **원고 수록 형태:** "점검 시간 XX% 단축, 재현율 XX%" 실측치 + 실험 사진
- 일정상 최소 버전: 1인 실측이라도 반드시 수치 확보 (v1.0의 "90% 효율화" 같은 무근거 주장 제거)

**실험 B: 라벨 열화 강건성 (FR-2.6 이중 대조 효과 정량화)**
- 셋업: 동일 서가에서 라벨을 단계적으로 열화(정상 → 일부 가림/잘림 → 백지 훼손) 처리한 이미지 세트
- 측정: 열화 수준별 **① 청구기호 단독 판정 정확도 ② 제목 이중 대조 추가 시 정확도(복구율) ③ 오탐률(0 유지 확인)**
- 기대 결과: "라벨 훼손 시 청구기호만으론 판독 불가하나, 제목 이중 대조로 복구율 XX%" — 샘플에서 라벨 백지 책을 제목만으로 복구·정확 판정하는 것을 이미 확인
- **원고 가치:** 현장 관찰(청구기호 잘림) → 설계 개선 → 실측 복구율로 이어지는 완결된 서사 (창의성·완성도)

**분석: 오배열 × 대출 데이터 상관 (FR-0.4)**
- **1순위:** **대림도서관 솔로몬 실제 장서·대출 데이터**로 상관분석 실행 → 파일럿이 아닌 실데이터 결과를 원고에 수록(적합성·창의성 강화). 익명·집계 형태 준수.
- **백업:** 솔로몬 반출이 제한될 경우 정보나루 개방 통계 + 시연 데이터로 분석 방법론과 파일럿 결과 제시, "본 도입 시 검증 가능"으로 서술.

---

## 8. 실행 일정 (2026. 7. 2. ~ 8. 28.)

| 주차 | 기간 | 목표 | 완료 기준 (verify) |
| :--- | :--- | :--- | :--- |
| W1 | 7/2~7/6 | **인증키 3건 신청(정보나루·국중·솔로몬 접근확인, 즉시)**, 공개 spine 데이터셋 수집, 사서 팀원 통해 실제 서가 촬영·장서 데이터 확보 경로 확정 | 인증키 발급 or 신청 완료, 솔로몬 반출 범위 확인, 이미지 1,000장+ |
| W2 | 7/7~7/13 | 자체 촬영·라벨링 300장, YOLO 1차 학습, ONNX 브라우저 추론 PoC | 폰 브라우저에서 책등 박스가 그려짐 |
| W3 | 7/14~7/20 | OCR 서버 구축, 청구기호 파싱·KDC 비교·LIS 판정 구현 + 유닛테스트 | 정지 사진 1장 → 오배열 빨간 박스 E2E 동작 |
| W4 | 7/21~7/27 | 실시간 파이프라인(트래킹·스무딩), 이용자 모드(검색→초록 박스), 정보나루 API 연동 | 실기기 라이브 스캔 데모 성공 |
| W5 | 7/28~8/3 | 대시보드, 매쉬업 분석(FR-0.4), **검증 실험 실시**, 시연 영상 촬영 | 실측 수치 확보, 백업 영상 완성 |
| W6 | 8/4~8/7 | **원고 집필·hwp 편집·제출** (버퍼 포함) | 8/6 제출 완료 목표 (마감 하루 전) |
| — | 8/8~8/23 | 완성도 보강: OCR 정확도 개선, 대시보드 폴리싱, 예상 질의 30문항 준비 | |
| W7 | 8/24~8/26 | 1차 통과 시: 발표자료 15쪽, 증명서류, 데이터·모델 1식 패키징 제출 | 8/26 24:00 前 제출 |
| W8 | 8/28 | **대면 발표·라이브 시연** (미니 서가 지참) | |

> **역할 분담(2인):** 개발자 = [모델/비전 · 로직/백엔드 · 앱]. 사서 팀원 = [솔로몬 데이터 확보·전처리 · 실제 서가 촬영·라벨링 협조 · KDC 배열 규칙 검수 · 현장 점검시간 실험 · 원고의 도서관 실무 관점 서술]. 사서 팀원의 도메인 지식은 창의성·적합성 심사에서 강력한 차별점.

---

## 9. 리스크 및 폴백 전략

| 리스크 | 확률 | 폴백 |
| :--- | :--- | :--- |
| 한국어 청구기호 OCR 정확도 미달 | 높음 | ① 청구기호 정규식 후처리 강화 ② CLOVA OCR 교체 ③ 최후: 시연은 통제 조명 + 표준 라벨 서가로 한정하고 한계를 원고에 명시 |
| **청구기호가 부분만 보임/훼손** (얇은 책 라벨 감김, 선반 턱 가림 — 대림 현장 확인) | 높음 | **구조 인지 매칭 + 제목 이중 대조** (샘플에 구현·검증 완료): ① 조각이 기준표에서 유일하면 확정 ② 분류번호가 읽히면 동일 분류 후보로 좁혀 저자기호 퍼지 매칭 ③ **라벨 실패·모호 시 책등 제목 OCR→기준표 서명 매칭으로 복구** ④ 둘 다 실패하면 '판독불가' 처리(오탐 금지). 검증: 라벨 백지 훼손 책을 제목만으로 복구·정확 판정 확인 |
| 실시간 파이프라인 성능 미달 | 중간 | **"한 컷 스캔 모드"**(사진 1장 촬영 → 2초 내 분석 결과 오버레이)를 기본 모드로 승격. 사용자 가치는 동일하고 구현 난도는 급감 |
| 현장 시연 사고 (조명·네트워크) | 중간 | ① 미니 서가 + 조명 지참 ② 오프라인 폴백(탐지는 온디바이스라 네트워크 무관, OCR 결과 캐시) ③ 사전 녹화 시연 영상 |
| 인증키 발급 지연 | 낮음 | 정보나루 개방 데이터(CSV 다운로드)는 키 없이 확보 가능 → 분석 파트 선진행 |
| 지재권 이슈 (ShelvAR가 미국 특허로 상용화 중단된 전례) | 낮음 | ShelvAR를 막은 특허는 '태그 기반' 방식 관련 미국 특허 → LibAR는 태그 미사용(OCR 방식)이고 국내 공모전 출품이므로 직접 리스크 낮음. 단, 요강상 지재권 책임은 응모자에게 있으므로 원고에서 태그 부착 방식 언급은 '차별화 대상'으로만 서술 |
| 실제 도서관 협조 불가 | 중간 | 모의 서가(개인 장서+중고서점 도서 50~100권에 청구기호 라벨 자체 부착)로 전 기능 시연 가능하도록 설계 |

### 9.1 적용 범위(Scope)와 한계 — 정직한 명시 (완성도·전문성 가점 전략)

> **원칙:** "모든 자료에 다 된다"는 과장은 감점. **작동 범위를 명확히 긋고 한계를 향후 과제로 서술**하는 것이 2차 발표심사 완성도·활용성 점수에 유리. 현직 사서 팀원의 도메인 서술로 설득력 강화. (원고 'Ⅵ. 기대효과·한계·확산'에 수록)

**전제(중요) — 본 시스템은 책을 '생김새'로 식별하지 않는다.**
YOLO는 책등 위치를, OCR은 **청구기호 라벨 텍스트**를 읽어 순서를 판정한다. 책의 정체성은 도서관이 이미 청구기호로 부여했으므로, 시스템은 그 라벨을 **읽기만** 한다. → 따라서 아래는 대부분 **문제가 아님**:

| 흔한 우려 | 판정 | 근거 |
| :--- | :--- | :--- |
| 같은 제목 여러 판본/개정판 | 문제 아님 | 판본이 뭐든 라벨 청구기호만 읽고 순서 비교. 판차·권차는 청구기호에 반영됨 |
| 외서·고전 다출판사 | 문제 아님 | 물리적 책마다 청구기호 부여됨 → 그대로 읽음 |
| 커버(재킷) 탈착 | 대체로 문제 아님 | 청구기호 라벨은 책등 하단에 직접 부착·보호필름 처리 → 재킷과 무관 |
| 책 다양성(각양각색) | 오히려 유리 | 다양한 책등은 탐지 모델 학습을 더 강건하게 함. 식별은 라벨로 |

**작동 전제 조건 (Scope IN):**
- 청구기호 라벨이 부착되고 **책등이 보이도록(spine-out) 일렬 배가**된 서가
- 실증 대상: **일반 단행본 서가**(예: 문학 800·사회과학 300번대 등)

**현재 한계 (Scope OUT / 향후 과제):**
1. **어린이 그림책·유아자료** — 책등이 좁고 판형이 천차만별이며, **면출(face-out)·바구니·수납장 배가**가 많아 'spine-out 일렬' 전제가 성립하지 않음 → **적용 범위에서 제외, 향후 과제로 명시**
2. **라벨 훼손·변색·보호필름 반사(글레어)·초소형 라벨** → OCR 오독 가능. **신뢰도 미달 시 '판독 불가(회색)'로 분리 표시**하여 오탐을 만들지 않음(미탐이 오탐보다 안전). 후처리 교정 + 필요 시 CLOVA OCR 교체
3. **라벨 미부착·비표준 배가 자료**(귀중본·특수자료 등) → 대상 외

---

## 10. 제출물 패키지 (2차 발표심사용 "데이터 및 모델 1식")

- `dataset/`: 책등 학습 데이터셋 (라이선스 명기) + 자체 촬영분
- `model/`: YOLO 가중치(.pt, .onnx), 학습 설정, 검증 리포트
- `analysis/`: 정보나루 데이터 수집·전처리·상관분석 Jupyter 노트북
- `src/`: 서비스 소스코드 (GitHub 저장소)
- `demo/`: 시연 영상(3분), 라이브 데모 URL(QR)

---

## 11. 원고(A4 15매) 목차 설계 — 심사기준 역매핑

요강 요약문 양식(활용데이터/활용기술/추진과정/결과물/기대효과)과 목차 순서(Ⅰ.→1.→가.→1)) 준수.

| 장 | 내용 | 분량 | 공략 심사항목 |
| :--- | :--- | :--- | :--- |
| 요약문 | 양식 5개 항목 그대로 | 1매 | (필수) |
| Ⅰ. 서론 | 문제 정의(통계 인용) + **선행사례 분석(§1.2 표: SHELF EYE·ShelvAR·RFID 로봇·소비자 앱)과 차별점** — 국내외 사례를 먼저 인정하고 공백을 짚는 구조 | 2매 | 적합성·창의성 |
| Ⅱ. 활용 데이터 | §4 표 + 수집·전처리 과정 | 2매 | **적합성 40** |
| Ⅲ. 시스템 설계 | 아키텍처, 탐지·OCR·LIS 알고리즘 | 3매 | 창의성 30 |
| Ⅳ. 구현 결과 | 스크린샷, 실험 실측치(§7), 대시보드 | 3.5매 | 기대효과·(2차)완성도 |
| Ⅴ. 매쉬업 분석 | 오배열×대출 상관, 위험도 스코어 | 1.5매 | 적합성·창의성 |
| Ⅵ. 기대효과·한계·확산 | 비용 0원 도입 모델, **적용 범위·한계(§9.1: 판본은 비문제/그림책은 향후 과제)**, 전국 확산 시나리오, 정보취약계층 기여 | 1.5매 | **기대효과 30·완성도** |
| 참고문헌·부록 | APA 스타일 | 0.5매 | (형식 준수) |

**형식 체크리스트:** hwp 저장 / 휴먼명조·지정 폰트 크기 / 여백 규정 / 쪽번호 / 표·그림 출처 명기 / **개인정보 0건 확인** / 파일명 `[서비스 아이디어 제안]LibAR(신청자명).hwp`

---

## 12. AI 기술 사용 명세 (요강 필수 기재사항)

원고에 아래를 명시:
- 객체 탐지: Ultralytics YOLO26n(폴백 YOLO11n) 파인튜닝, NMS-free/OBB — 산출물: 책등 경계박스 — 학습 데이터 출처·라이선스 및 mAP 실측치 병기
- 문자 인식: PaddleOCR korean(또는 CLOVA OCR) — 산출물: 청구기호 텍스트
- 개발 보조: 생성형 AI(Claude) 코드 작성 보조 — 산출물: 서비스 소스코드 일부
- 학습 데이터 출처·라이선스 및 각 모델의 성능 실측치 병기

---

## 13. 성공 지표 (실측 가능한 것만)

1. **점검 효율:** 모의 서가 100권 기준 육안 대비 점검 시간 단축률 (목표 ≥ 60%, §7 실험으로 실측)
2. **탐지 품질:** 책등 mAP@0.5 ≥ 0.85, 청구기호 판독 정확도 ≥ 90%, 오배열 판정 재현율 ≥ 90% (통제 환경)
3. **데이터 가치:** 오배열 로그 × 대출 데이터 상관분석 리포트 1건 + 점검 우선순위 스코어 모델 1건
4. **접근성:** QR 스캔 → 첫 탐지까지 15초 이내, 설치 0회
